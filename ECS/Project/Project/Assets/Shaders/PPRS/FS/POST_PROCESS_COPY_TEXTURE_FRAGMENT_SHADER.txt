#version 430 core

in vec4 texture_coordinates;

layout(binding = 1) uniform sampler2D TextureSampler0;

out vec4 color;

void main()
{
	vec4 finalColor = texture2D(TextureSampler0, vec2(texture_coordinates.x, texture_coordinates.y));  
    color = vec4(finalColor.xyz, 1.0);
}